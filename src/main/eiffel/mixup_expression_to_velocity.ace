system "x2v"

root
    MIXUP_EXPRESSION_TO_VELOCITY: make

default
    assertion(require)
    assertion_flat_check(yes)
    debug(no)
    trace(yes)

cluster
    mixup: "./loadpath.se"

    standard: "${path_liberty_core}loadpath.se"
        default
            assertion(no)
        end

end
